#!/bin/sh
# Pre-push hook: full quality gate (runs before every push).
# Install with: make setup
set -e
cd "$(git rev-parse --show-toplevel)/SAGE" 2>/dev/null || cd "$(git rev-parse --show-toplevel)"
make check
